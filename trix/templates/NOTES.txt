trix-agent has been installed!

1. Check the deployment status:
   kubectl get pods -l "app.kubernetes.io/name={{ include "trix.name" . }}" -n {{ .Release.Namespace }}

2. View the logs:
   kubectl logs -l "app.kubernetes.io/name={{ include "trix.name" . }}" -n {{ .Release.Namespace }} -f

{{- if .Values.saas.enabled }}

SaaS integration is enabled. The agent will sync:
- Vulnerability data from Trivy Operator
- Compliance benchmark results
- Workload inventory

Endpoint: {{ .Values.saas.endpoint }}
{{- else }}

SaaS integration is disabled. Enable it in values.yaml:
  saas:
    enabled: true
    endpoint: "https://app.trixsec.dev"
    apiKey: "your-api-key"
{{- end }}

For more information, visit: https://github.com/trixsec-dev/trix
